# Personal AI Workbench

Personal AI Workbench 的领域词汇表，记录工作区绑定与跨功能操作中必须保持一致的概念。

## 工作区绑定

**工作区**：一组由当前 Vault 身份确定、用于保存项目状态、学习内容、摘要和备份数据的持久化上下文。
_避免_：目录、项目目录、Vault 本身

**绑定工作区**：当前 Vault 身份与一个工作区之间的稳定关联。
_避免_：当前目录、活动目录

**预期绑定**：一次操作开始时捕获的绑定工作区身份，操作完成前必须保持不变。
_避免_：快照目录、旧工作区

**绑定工作区操作**：必须在预期绑定下完成的读取或变更，若绑定在操作期间改变则操作失败。
_避免_：普通请求、工作区任务

## 数据提供

**本地数据提供方**：为工作区提供持久化数据或备份数据的具体适配器。
_避免_：数据源、后端服务

**只读解析**：只读取现有绑定，不创建工作区或持久化目录的解析方式。
_避免_：无副作用请求

## 运行时组合

**工作区运行时**：以显式 read/write 模式解析工作区、捕获和守卫预期绑定，并按领域入口组合仓库与备份提供方的服务边界。
_避免_：Vite 插件辅助函数、HTTP 响应器
